<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Inventory Management</title>

    <style>
        body {
            font-family: 'Arial', sans-serif;
            background-color: #f4f4f4;
            margin: 0;
            padding: 0;
        }

        header {
            background-color: #333;
            color: white;
            padding: 10px;
            text-align: center;
            height: 2rem;
            display: flex;
            justify-content: space-between;
        }

        header h1 {
            color: white;
            margin: 0;
        }

        #record-details {
            max-width: 600px;
            margin: 20px auto;
            padding: 20px;
            background-color: #fff;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
        }

        form {
            display: grid;
            grid-gap: 10px;
        }

        label {
            font-weight: bold;
        }

        input {
            width: 100%;
            padding: 8px;
            box-sizing: border-box;
        }

        .button-row {
            display: flex;
            justify-content: space-between;
            margin-top: 20px; /* Adjust the margin as needed */
        }

        .cancel-btn {
            background-color: red;
            color: white;
            border: none;
            cursor: pointer;
        }

        button {
            background-color: #4caf50;
            color: #fff;
            padding: 10px;
            border: none;
            cursor: pointer;
        }

    </style>
</head>

<body>
    <header>
        <h1>Update Product Data</h1>
    </header>
    <div id="record-details">
        <form id="updateForm" action="/updateRecordInventory" method="POST">
            <input hidden type="text" id="id" name="id" value="<%= recordId %>">

            <label for="product_id">Product ID:</label>
            <input type="text" id="product_id" name="product_id" value="<%= recordDetails.product_id %>">

            <label for="product">Product:</label>
            <input type="text" id="product" name="product" value="<%= recordDetails.product %>">

            <label for="quantity">Quantity:</label>
            <input type="text" id="quantity" name="quantity" value="<%= recordDetails.quantity %>">

            <label for="amount">Amount:</label>
            <input type="text" id="amount" name="amount" value="<%= recordDetails.amount %>">

            <div class="button-row">
                <button type="submit">Save</button>
                <button class="cancel-btn" onclick="redirectToRecordWorkTime()">Cancel</button>
            </div>
        </form>
    </div>

    <script>
        function redirectToRecordWorkTime() {
            window.location.href = '/dashboard'
        }
    </script>
</body>

</html>
