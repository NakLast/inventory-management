<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Inventory Management</title>

    <style>
        body {
            font-family: 'Arial', sans-serif;
            background-color: #f4f4f4;
            margin: 0;
            padding: 0;
        }

        header {
            background-color: #333;
            color: white;
            padding: 10px;
            text-align: center;
            height: 2rem;
            display: flex;
            justify-content: space-between;
        }

        header h1 {
            color: white;
            margin: 0;
        }

        #record-details {
            max-width: 600px;
            margin: 20px auto;
            padding: 20px;
            background-color: #fff;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
        }

        form {
            display: grid;
            grid-gap: 10px;
        }

        label {
            font-weight: bold;
        }

        input {
            width: 100%;
            padding: 8px;
            box-sizing: border-box;
        }

        .button-row {
            display: flex;
            justify-content: space-between;
            margin-top: 20px;
            /* Adjust the margin as needed */
        }

        .cancel-btn {
            background-color: red;
            color: white;
            border: none;
            cursor: pointer;
        }

        button {
            background-color: #4caf50;
            color: #fff;
            padding: 10px;
            border: none;
            cursor: pointer;
        }
    </style>
</head>

<body>
    <header>
        <h1>Update Record Work Time</h1>
    </header>
    <div id="record-details">
        <form id="updateForm" action="/updateRecordWorkTime" method="POST">
            <input hidden type="text" id="id" name="id" value="<%= recordId %>">

            <label for="employee">Employee:</label>
            <input type="text" id="employee" name="employee" value="<%= recordDetails.employee %>" readonly>

            <label for="start_time">Start Time:</label>
            <input type="text" id="start_time" name="start_time" value="<%= recordDetails.start_time %>">

            <label for="end_time">End Time:</label>
            <input type="text" id="end_time" name="end_time" value="<%= recordDetails.end_time %>">

            <div class="button-row">
                <button type="submit">Save</button>
                <button class="cancel-btn" onclick="redirectToRecordWorkTime()">Cancel</button>
            </div>
        </form>
    </div>

    <script>
        function redirectToRecordWorkTime() {
            window.location.href = '/recordWorkTime'
        }
    </script>
</body>

</html>